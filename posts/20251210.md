---
title: 刷題紀錄 - Binary Tree Right Side View
cat: LeetCode
---

- 上一整天班還要回來刷題真D累，精神狀態👇🏻


![累累](https://i.imgur.com/hjfdJVf.jpeg)

- **難度**：Medium  
- **題目連結**：https://leetcode.com/problems/binary-tree-right-side-view

---

### 思考過程

1. 題目直接寫了 BFS（廣度優先搜尋），希望面試也有這麼好心的提示
2. 要找出二元樹的最右邊的節點，回傳 [node0, ... , nodeN]
3. BFS 先把每層的節點都放到 Queue 裡面，然後先進先出逐一取出節點，繼續放子節點到 Queue 裡面
4. 所以只要知道每層最後一個節點是哪個就可以解出答案
5. 從最經典的 BFS 開始修改：
```python
def bfs(root):
    if not root:
        return

    q = deque([root])

    while q:
        node = q.popleft()
        print(node.val)       # 這裡可以放一些節點處理的邏輯

        if node.left:
            q.append(node.left)

        if node.right:
            q.append(node.right)
```
6. while 迴圈裡面要紀錄每層有多少個節點，用一個 for 迴圈來遍歷同一層的所有節點
```python
while q:
    # 紀錄每層有多少個 node
    size = len(q)
    # 走 BFS 從左到右逐一檢查，把每層的最後一個 node 放到 right_nodes
    for i in range(1, size+1):
        # 新增子節點跟加入最右邊節點(i == size)的邏輯
```
7. 搞定

### 複雜度分析

- **時間複雜度**：
> 這次用 List 來模擬 Queue，pop(0)這種做法並非常規的 Queue 操作。這作法每次 Pop 都會需要把後面的元素往前搬動造成浪費，時間複雜度變成 $O(n^2)$。如果正確使用 Queue 的話時間是 $O(n)$
- **空間複雜度**：
> 最差是歪斜樹紀錄所有節點 $O(n)$。

---

## 程式碼（Python 解法）

```python
class Solution:
    def rightSideView(self, root: Optional[TreeNode]) -> List[int]:
        if not root:
            return []

        # 紀錄最右邊的 node.val
        right_nodes = []
        # root 先放進去
        q = [root]

        # 開始 BFS
        while q:
            # 紀錄每層有多少個 node
            size = len(q)

            # 在 for 迴圈裡面透過 size，把每一層處理完
            for i in range(1, size+1):
                node = q.pop(0)
                if node.left:
                    q.append(node.left)
                if node.right:
                    q.append(node.right)
                # 若是這層的最後一個    
                if i == size:
                    right_nodes.append(node.val)

        return right_nodes
```

---

## 小結 & 反思

- **這題的重點：** 在經典的 BFS 方法裡面加了一層 for 迴圈，搭配 size 找出最後一個節點
---